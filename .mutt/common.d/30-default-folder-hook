# All matching hooks are executed in the order they appear, so this
# default needs to come before folder-specific ones and before
# mutt.pub/.mutt/common.d/31-default-folder-hook.pub
folder-hook . source ~/.mutt/folders/DEFAULTS
