#!/bin/bash

. $ZDOTDIR/.zsh/functions/enable_nullglob

for profile in "$@"; do
  for hook in $( . $ZDOT_FIND_HOOKS ".mutt/${profile}.d" ); do
    cat <<EOF
#########################################################################
# $hook follows:

EOF
    cat "$hook"
    echo
  done
done

