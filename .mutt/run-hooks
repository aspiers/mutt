#!/bin/bash

. $ZDOTDIR/.zsh/functions/enable_nullglob

for profile in "$@"; do
    . $ZDOT_FIND_HOOKS ".mutt/${profile}.d"
done | sort-by-filename |
while read hook; do
    cat <<EOF
#########################################################################
# $hook follows:

EOF
    cat "$hook"
    echo
done

