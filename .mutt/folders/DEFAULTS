bind pager z noop

## N.B. This gets altered by ~/.mutt/{scoring,mailing-list} !
  set index_format="%Z%3C %(%b %d) %-21.21F %s"

  set attribution="%n (%a) wrote:"
  set indent_string="> "
  set pgp_verify_sig
  set pager_index_lines=8
unset signature
