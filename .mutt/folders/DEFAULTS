# N.B. These defaults can get altered by other files in
# ~/.mutt/folders

bind pager z noop

  set index_format='%Z%4C %[%b %d] %-18.18f %s%>  %g%c'
  set pgp_verify_sig
  set pager_index_lines=12
unset signature

source ~/.mutt/colours/DEFAULT
